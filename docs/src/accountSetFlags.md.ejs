## accountFlags

The following are integer (as opposed to bit) flags that can be set for particular transactions in the AccountSet SetFlag or ClearFlag field

`accountFlags.AccountSet.asfRequireDest`: Require a destination tag to send transactions to this account.
`accountFlags.AccountSet.asfRequireAuth`: Require authorization for users to hold balances issued by this address. Can only be enabled if the address has no trust lines connected to it.
`accountFlags.AccountSet.asfDisallowXRP`: XRP should not be sent to this account. (Enforced by client applications, not by rippled)
`accountFlags.AccountSet.asfDisableMaster`: Disallow use of the master key pair. Can only be enabled if the account has configured another way to sign transactions.
`accountFlags.AccountSet.asfAccountTxnID`: Track the ID of this account's most recent transaction.
`accountFlags.AccountSet.asfNoFreeze`: Permanently give up the ability to freeze individual trust lines or disable Global Freeze. This flag can never be disabled after being enabled.
`accountFlags.AccountSet.asfGlobalFreeze`:	Freeze all assets issued by this account.
`accountFlags.AccountSet.asfDefaultRipple`: Enable [rippling](https://xrpl.org/rippling.html) on this account's trust lines by default.
`accountFlags.AccountSet.asfDepositAuth`:Enable Deposit Authorization on this account.