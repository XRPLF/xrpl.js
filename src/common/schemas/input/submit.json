{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "submitParameters",
  "type": "object",
  "properties": {
    "signedTransaction": {
      "$ref": "blob",
      "description": "A signed transaction as returned by [sign](#sign)."
    },
    "failHard": {
      "$ref": "boolean",
      "description": "(defaults to false) If true, and the transaction fails locally, do not retry or relay the transaction to other servers"
    }
  },
  "additionalProperties": false,
  "required": ["signedTransaction"]
}
