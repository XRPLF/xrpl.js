To run integration tests:
1. Run rippled-standalone node.
  * Either with docker `docker run -p 6006:6006 -it natenichols/rippled-standalone:latest`
  * Download and build rippled and run `./rippled -a`
2. Run `yarn test:integration` or `yarn test:browser`
